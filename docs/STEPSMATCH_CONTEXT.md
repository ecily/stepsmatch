# StepsMatch.com - Projektkontext

Stand: 2026-06-23, lokale Bestandsaufnahme in `C:\coding\stepsmatch`.

## 1. Projektgrenzen

- Dieses Projekt ist ausschliesslich StepsMatch.com.
- Keine Vermischung mit Kaufklug oder anderen Projekten.
- Strategieentscheidung vom 2026-06-21: StepsMatch.com bleibt das technische Labor fuer orts-, zeit- und interessenbasiertes Matching. Es wird nicht als echter Graz-Rollout, lokaler Anbieter-Marktplatz, Dealportal, Camino-App oder Pilgerprodukt positioniert. Zweck sind technische Validierung, Field-Tests, Logging und UX-Erkenntnisse.
- Consent-first Onboarding-Journey als erster Architektur-Schritt dokumentiert in `docs/STEPSMATCH_CONSENT_ONBOARDING.md`. StepsMatch bleibt neutral fuer lokale interessenbasierte Hinweise/Angebote in Graz und Umgebung. Standort, Background Location und Push duerfen erst nach klarer Erklaerung abgefragt werden; die Journey umfasst Willkommen, Interessen, Standort, Push, Hintergrundstandort und Statusscreen. Noch kein Code.
- Urbanes Kategorie-/Subkategorie-Modell dokumentiert in `docs/STEPSMATCH_URBAN_TAXONOMY.md`. StepsMatch bleibt urban/lokal fuer Graz und Umgebung; die Taxonomie ist Grundlage fuer Demo-Markt, Onboarding, Matching, Push-Prioritaet und Admin-KPIs. Keine Codeaenderung.
- Observability/Admin-KPI-Modell dokumentiert in `docs/STEPSMATCH_OBSERVABILITY.md`. Es baut auf consent-first Onboarding und urbaner Taxonomie auf; Ziel sind erklaerbare Feldtests, Admin Panel und Pitch-KPIs. Noch kein Code, keine DB-Mutation.
- Demo-Markt Zone B dokumentiert in `docs/STEPSMATCH_DEMO_MARKET_ZONE_B.md` mit Fokus Graz-Nord/Goesting/Andritz. Reale Anbieter bleiben nur Kandidaten ohne Partnerclaims; oeffentliche Orte koennen redaktionelle Hinweise werden. Keine Codeaenderung, keine DB-Mutation.
- Pre-Alpha-Demo-Markt-Entscheidung vom 2026-06-23: Die reine Zone-B-Dichte ist zu klein; Grundlage fuer spaetere Demo-Datenbefuellung ist nun `docs/STEPSMATCH_DEMO_MARKET_SEED_GRAZ_NORD_V1.md` mit mindestens 100 realen Demo-Locations. Fokus: Graz-Nord/Goesting/Andritz plus ausdruecklich 8111 Gratwein-Strassengel. Planungsbegriff: `real_demo_location` fuer reale, oeffentlich auffindbare Orte/Anbieter ohne Partnerclaim, Rabatt, Angebot, Preis oder Verfuegbarkeitsversprechen. Keine Codeaenderung, keine DB-Mutation.
- Pitch-Demo-Matchmodell dokumentiert in `docs/STEPSMATCH_PITCH_DEMO_CURATED_MATCH_MODEL_V1.md`. Entscheidung vom 2026-06-23: Der 110er Seed ist Rohmaterial und wird nicht blind importiert. Der pitchfaehige MVP braucht getrennte `DemoLocation`- und `MatchableDemoContent`-Ebenen; Matching basiert auf Interesse, Radius, Laufzeit, geografischer Gueltigkeit, Sichtbarkeit, Push-Regeln und Dedupe/Cooldown. Provider sollen spaeter Radius und Laufzeit pro Inhalt innerhalb Systemgrenzen steuern koennen. Kategorie-Luecken bei Ruhe & Pause sowie Kultur & Events vor Beta beachten. Keine Codeaenderung, keine DB-Mutation.
- Pitch-MVP-Gap-Audit dokumentiert in `docs/STEPSMATCH_PITCH_MVP_GAP_AUDIT.md`. Entscheidung vom 2026-06-23: Ampel `gelb`. Gruen sind der technische Kern aus Heartbeat, Radius-/Zeitfenster-Matching, Push-Dedupe, Mobile Feed/Karte/Route/Arrival und Anbieter-Offer-Formularen. Gelb/rot sind DemoLocation/MatchableDemoContent, Visibility, Push-Eligibility, Demo-Labels, Provider-Radius-Persistenz, Match-Reasons und KPI-Oberflaeche. Naechster sicherer Schritt: additive Backend-Policy-/Datenmodell-Felder ohne Import, DB-Mutation oder Mobile-Heartbeat-Risiko.
- Ultreia.app ist ein spaeteres, eigenstaendiges Produkt fuer Camino-Pilger mit eigenem Repo/Kontext, eigener Marke, eigenen Daten, eigener Produktlogik und eigenen Entscheidungen. StepsMatch darf nur bewaehrte technische Bausteine und Erkenntnisse liefern; keine Ultreia-spezifischen Commits, Deploys, Daten, Kategorien, Pilger-Sprache, Camino-Daten oder Branding-Aenderungen in StepsMatch.
- Graz/Judendorf-Tests sind Pre-Beta-/Labor-Field-Tests mit wenigen Freunden, klar gekennzeichneten Demo-Angeboten/Testhinweisen und ohne falsche Anbieterclaims, ungekennzeichnete Fake-Angebote, fremde Logos/Bilder ohne Rechte, Verkaufsversprechen oder Partner-/Umsatzversprechen.
- Labor-Erfolg bedeutet technische Stabilitaet, Push-Relevanz, nachvollziehbare Match-Entscheidungen, verstaendliche UX, keine Spam-Wahrnehmung und auswertbare Logs; nicht Anbieter-, Markt- oder Umsatznachweis.
- Technische Laborprioritaeten: Logging der Match-/Push-Kette, Push-/Notification-Haertung, User/PushToken-Kopplung, NotificationLogs/Push-Receipts, Match-/Nicht-Match-Reasons, Demo-Content-Kennzeichnung, Push-Frequenzkontrolle/Anti-Spam, Feed-Retry/Fehlerdiagnose, Battery-/Heartbeat-Beobachtung und UX-Klarheit fuer Tester.
- Fuer kommende Tests sollen Nutzer/Device pseudonymisiert, aktive Interessen, Standortkontext, Kandidaten im Radius, Match-/Nicht-Match-Grund, Entfernung, Zeitfenster, App-Zustand, Push/Local-Notification-Ausloesung oder Unterdrueckung, Duplicate Suppression, Rate Limiting, Offer-Oeffnung, Karte, Directions, Network Errors, Heartbeat-Zustand, PushToken-Zustand und Notification Receipts soweit verfuegbar nachvollziehbar sein.
- Lokaler Pfad: `C:\coding\stepsmatch`
- Repo/Remote: `origin https://github.com/ecily/stepsmatch.git`
- Branch: `main` tracking `origin/main` ohne sichtbaren ahead/behind-Hinweis im Status.
- letzter Commit: `d06065b00f6173cb3dc4228744147aff4ea7e983` (`d06065b`), 2026-04-01 15:25:25 +0200, `fix(frontend): handle email verification flow on provider login/register`
- relevante Tags:
  - `v1.0.0-stable-baseline-2026-02-20`
  - `meilenstein-2025-09-19`
  - `prod-safepoint-2025-09-16`
  - `meilenstein-2025-09-14`
  - `stable-geo-2025-09-07`
  - `stable-20250825-1050`
  - `baseline-push-2025-08-22`
  - `safe-pre-deploy`
  - `push-notifications-ok`
  - `v1.0-stable`
- Git-Arbeitsbaum bei Bestandsaufnahme: deutlich dirty. `git status --porcelain` zeigt 174 Eintraege: 63 tracked modified/deleted und 111 untracked.
- Relevante uncommitted Aenderungen betreffen Backend-Routen/Modelle, Frontend-UX/API-Resolver und viele Mobile-Dateien fuer Push, Location, Navigation, Permissions und Android-Konfiguration.
- Viele untracked Dateien sind Screenshots, UI-Dumps, Retest-Logs und temporäre Skripte (`screen_*.png`, `retest_*.png`, `temp_*.mjs`, `temp_*.ps1`, `uidump*.xml` usw.).
- Stabilisierungsstand nach Cleanup am 2026-06-17: Kontextdatei und `.gitignore` wurden committed; 97 lokale Test-/Screenshot-/Dump-/Temp-Artefakte wurden geloescht. Verbleibend: 62 tracked Produkt-/Config-Aenderungen und 17 untracked Dateien.
- Live-Status laut Betreiberhinweis vom 2026-06-17: Frontend und Backend laufen bereits auf DigitalOcean; MongoDB ist live angebunden. Kein Push, Deploy oder DB-Mutation ohne explizite Freigabe.
- Backend-Stabilisierungsstand am 2026-06-17: `backend/server.js` LAN-Startup-Logging wurde isoliert committed (`3fd8df1`); Auth/User/Tester wurde isoliert committed (`4524ffd`). Die CORS-Domains in `backend/server.js` sind wieder StepsMatch-Domains. Push-Dateien wurden auf HEAD zurueckgefuehrt, weil die offenen Diffs nur Encoding-/Mojibake-Rauschen enthielten. Location wurde auf HEAD zurueckgefuehrt und nur der sichtbare Fallback-Text `Angebot in deiner Naehe` korrigiert (`5a7a7d0`). Der Backend-Worktree ist damit ohne offene tracked Diffs.
- Backend-Env-Beispielstand am 2026-06-19: `backend/routes/location.js` wurde erneut sauber geprueft. `backend/.env.example` wurde geprueft; der alte `ultreia.app`-Beispielwert in `CORS_ORIGINS` wurde durch StepsMatch-Beispieldomains ersetzt. Es wurden keine echten Secrets in `backend/.env.example` erkannt. Kein Push, Deploy oder DB-Mutation.
- Backend-Script-Sicherheitsstand am 2026-06-19: Das hardcoded MongoDB-URI-/Branding-Risiko in `backend/scripts/ensure-indexes.mjs` wurde entfernt; das Script liest die URI nur noch aus Env (`MONGODB_URI`, `MONGO_URI`, `DATABASE_URL`) und bricht ohne URI mit sicherem Fehlertext ab. Keine DB-Scripts ausgefuehrt, kein Push, kein Deploy, keine DB-Mutation.
- Backend-Script-Abschlusspruefung am 2026-06-19: Die erlaubten Maintenance-Scripts/Jobs wurden statisch auf Secret-/URI- und Branding-Risiken geprueft. Keine hardcoded MongoDB-URI oder alten produktiven Branding-Reste in den geprueften Scriptdateien erkannt; Seed-/Index-/Poller-Scripts bleiben DB-mutierend und wurden nicht ausgefuehrt. Kein Push, kein Deploy, keine DB-Mutation.
- Backend-Alpha-Login am 2026-06-19: `POST /api/users/login` unterstuetzt einen env-gesteuerten Alpha-Master-Login fuer genau die per Env konfigurierte Alpha-E-Mail, nur wenn `ALPHA_MASTER_LOGIN_ENABLED=true` gesetzt ist. Kein hardcoded Passwort, kein beliebiges User-Impersonation-Masterpasswort, keine DB-Mutation; Response bleibt im normalen Login-Format. `backend/.env.example` enthaelt nur Platzhalter fuer die Alpha-Env-Namen. Keine Secrets dokumentiert, kein Push, kein Deploy, keine Live-DB-Mutation.
- Frontend-Untracked-Stand am 2026-06-19: `frontend/.env.example` wurde als Example-Datei ohne echte Secrets geprueft und auf StepsMatch-Beispieldomains ausgerichtet. Untracked Frontend-/Legal-/Textdateien wurden auf StepsMatch-Branding korrigiert; Camino/CaminoShell-Assets bleiben bewusst uncommitted/zurueckgestellt. Kein Push, Deploy oder DB-Mutation.
- Frontend-Shell-Bereinigung am 2026-06-19: Navbar/Landing wurden von alten Camino/CaminoShell-Asset-Referenzen geloest; die verbleibenden untracked Drift-Assets wurden geloescht. Kein Push, kein Deploy, keine DB-Mutation, keine Mobile-/Android-Aenderung.
- Frontend-Low-Risk-Branding am 2026-06-19: `index.html`, `App.css`, `Pitch.jsx` und `WhyStepsMatch.jsx` wurden auf StepsMatch-Branding bereinigt. Keine Routing-/API-/Auth-/Provider-Logik geaendert; Frontend-Block bleibt wegen `axios.js`, `App.jsx` und Auth-/Provider-Flows offen. Kein Push, kein Deploy, keine DB-Mutation.
- Frontend-API-Client am 2026-06-19: `frontend/src/api/axios.js` wurde auf StepsMatch bereinigt: Env-first API-Basis, Produktionsfallback zur Live-Backend-API, keine Ultreia-/Camino-Host-Mappings, Tester-Key-Headerlogik erhalten. Keine Secrets dokumentiert, kein Push, kein Deploy, keine DB-Mutation.
- Frontend-Routing-Baseline am 2026-06-19: `frontend/src/App.jsx` wurde konservativ auf die Gate-Baseline zurueckgefuehrt; `BootGuard`, `TesterGate`, `/nda`, `/pitch` und `/home` bleiben erhalten. Nur die versionierte `/impressum` Route wurde ergaenzt. Kein Push, kein Deploy, keine DB-Mutation.
- Frontend-Legal/Cookie am 2026-06-19: Cookie-Hinweis und Datenschutzseite wurden auf StepsMatch-Branding geprueft; der alte Ultreia-Consent-Key wurde auf den StepsMatch-Namespace zurueckgefuehrt. Keine neuen Rechtsangaben erfunden, kein Push, kein Deploy, keine DB-Mutation.
- Frontend-Produktklarheit am 2026-06-19: Web-Copy wurde auf den StepsMatch-Kern ausgerichtet: standortbasiertes Angebots-Matching, Naehe + Interesse + aktives Angebot, Mobile-App als echter Core und interne Alpha statt oeffentlicher Beta-Versprechen. Camino-/Ultreia-Reste im Frontend wurden neutralisiert; Login/Register-Copy beschreibt Anbieterpflege ohne fachlichen Auth-/API-/Gate-Umbau. Keine Backend-/Mobile-/Android-Aenderung, keine Secrets, keine DB-Mutation.
- Frontend-Designschaerfung am 2026-06-19: Das Web-Frontend wurde optisch auf den StepsMatch-USP ausgerichtet: bildgetragener Hero mit `Angebote finden dich.`, klare Erklaerung von Naehe + Interesse + aktivem Angebot, neue globale Web-Farbwelt mit ruhigem Grund, dunkler Tinte, Signalgruen, Route-Blau, Korall und Teal sowie Inter/Space-Grotesk-Fonts. Die Aenderung ist frontend-only plus Kontextdoku; keine Backend-/Mobile-/Android-/Env-Aenderung, keine Secrets, kein Push, kein Deploy, keine DB-Mutation.
- Frontend-Relikt-Cleanup am 2026-06-19: Nicht gepushte Web-Copy und sichtbare Designbestandteile wurden weiter auf den neuen StepsMatch-Weg geschaerft. `PRE ALPHA` ist nun prominent im Hero sichtbar; Pitch-/Asset-/Dashboard-Texte vermeiden altes Suche-/Portal-/Deal-/Ads-Framing und erklaeren positiv Naehe + Interesse + aktives Angebot. Branding-Suche im Frontend bleibt ohne Ultreia-/Camino-Treffer. Keine Backend-/Mobile-/Android-/Env-Aenderung, keine Secrets, keine DB-Mutation.
- Frontend-Landing-Feinschliff am 2026-06-19: Hero wurde weiter auf sofortige USP-Verstaendlichkeit optimiert: H1 exakt `Angebote finden dich`, prominentes `PRE ALPHA`, kurze Erklaerung des internen Teststands und ein visuelles Matching-Signal fuer Naehe + Interesse + aktives Angebot. Aenderung bleibt Landing-/Style-/Text-/Docs-only; keine Backend-/Mobile-/Android-/Env-Aenderung, keine Secrets, keine DB-Mutation.
- Frontend-Copy-Feinschliff am 2026-06-19: Landing-Texte wurden weiter gekuerzt und menschlicher formuliert. Hero bleibt `PRE ALPHA` + `Angebote finden dich`; Subline, Chips, einfache Drei-Schritt-Erklaerung und Anbieter-Satz wurden ohne Design-/Layout-Umbau vereinfacht. Keine Backend-/Mobile-/Android-/Env-Aenderung, keine Secrets, keine DB-Mutation.
- Frontend-USP-Copy am 2026-06-19: Hero-Subline, Chips, Drei-Schritt-Erklaerung und Anbieter-Satz wurden final auf die Hintergrund-App-Logik verdichtet. Die optionale Impressum-Telefonzeile wird bei fehlender Telefonnummer nicht mehr angezeigt. Keine Backend-/Mobile-/Android-/Env-Aenderung, keine Secrets, keine DB-Mutation.
- Mobile-Runtime-Konfiguration am 2026-06-19: `mobile/lib/runtimeConfig.js` wurde versioniert, weil tracked Mobile-Code diese Datei importiert. Altbranding wurde entfernt; API-Basis bleibt Env-/Expo-Extra-first mit bewusstem Live-Backend-Fallback nur fuer interne Tests. Keine Push-/Heartbeat-/Geofence-Logik geaendert, kein Gradle Build, kein APK-Test, kein Push, kein Deploy, keine DB-Mutation.
- Mobile-Branding-Schnitt am 2026-06-19: Sichtbare Mobile-Texte, Notification-Labels und Theme-Kommentare wurden auf StepsMatch/neutral bereinigt. Android/Gradle/Manifest/HeartbeatService wurden nicht geaendert; `mobile/babel.config.js` bleibt wegen Production-Console-Removal zurueckgestellt. Kein Gradle Build, kein APK-Test, kein Push, kein Deploy, keine DB-Mutation.
- Mobile-Theme-Migration am 2026-06-19: `mobile/theme/tokens.js` wurde durch `mobile/theme/tokens.ts` ersetzt; `ThemeProvider.tsx` bleibt mit extensionless `./tokens` kompatibel. Keine Push-/Heartbeat-/Geofence-/Android-Aenderung, kein Gradle Build, kein APK-Test, kein Push, kein Deploy, keine DB-Mutation.
- Mobile-Babel-Konfiguration am 2026-06-19: `mobile/babel.config.js` wurde ohne Production-Console-Removal aufgenommen; Diagnose-Logs bleiben fuer den Background-Heartbeat-/Push-Audit erhalten. Keine Package-Dateien geaendert, kein Gradle Build, kein APK-Test, kein Push, kein Deploy, keine DB-Mutation.
- Mobile-Package-Bereinigung am 2026-06-19: Die ungenutzten `babel-plugin-transform-remove-console`-Package-Diffs wurden entfernt; Diagnose-Logs bleiben fuer den Background-Heartbeat-/Push-Audit erhalten. Kein npm install, kein Gradle Build, kein APK-Test, kein Push, kein Deploy, keine DB-Mutation.
- Mobile-HeartbeatService-Text am 2026-06-19: Sichtbare Foreground-Service-/Notification-Texte in `HeartbeatService.kt` wurden auf StepsMatch/neutral bereinigt. Keine Heartbeat-, Foreground-Service-, Permission-, Gradle-, Manifest- oder Package-ID-Logik geaendert; Package-ID/Signing bleiben separat zu entscheiden. Kein Build, kein APK-Test, kein Push, kein Deploy, keine DB-Mutation.
- Mobile-Package-/Signing-Baseline am 2026-06-19: Fuer den internen Geraetetest wurde `com.ecily.stepsmatch` als bewusste Test-/Baseline-Application-ID akzeptiert; Android `namespace` und native Kotlin-Packages bleiben vorerst `com.ecily.mobile`. `google-services.json` enthaelt einen passenden Client ohne dokumentierte Werte; Debug-Signing-Fallback fuer Release-APK ist fuer den heutigen internen Test akzeptiert, echtes Release-Signing wird spaeter separat gehaertet. Vor dem Geraetetest beide App-IDs (`com.ecily.mobile`, `com.ecily.stepsmatch`) deinstallieren. Kein Build, kein APK-Test, kein Push, kein Deploy, keine DB-Mutation.
- Mobile-Android-Maps-Key am 2026-06-19: Der hardcoded Google-Maps-Key wurde aus `AndroidManifest.xml` entfernt und durch den Manifest-Placeholder `GOOGLE_MAPS_ANDROID_API_KEY` ersetzt. `build.gradle` befuellt den Placeholder aus Env bzw. `mobile/.env` (`EXPO_PUBLIC_GOOGLE_MAPS_ANDROID_KEY`, fallback `GOOGLE_MAPS_ANDROID_API_KEY`). Der Directions-Key bleibt nur in Env/`.env` und nicht im Manifest. Keine Key-Werte dokumentiert; Key-Rotation/History-Cleanup bleibt vor Push oder oeffentlichem Release offen. Keine Heartbeat-/Push-/Geofence-/Permission-/Package-/Signing-Aenderung, kein Build.
- Mobile-Map-Sichtbarkeit am 2026-06-19: `NavigationMap.jsx` und `NavigationScreen.js` nutzen weiter eingebettete `react-native-maps` MapViews mit `PROVIDER_GOOGLE`; die Map-Flaeche wurde auf absolute Fill-Styles stabilisiert und der custom Map-Style fuer Standardkarten entfernt, damit die native Google-Basemap sichtbar bleibt. Keine Android-Konfiguration, keine API-/Push-/Heartbeat-/Geofence-/Permission-Logik geaendert; kein Gradle Build, keine APK-Installation.
- Mobile-Map-Debug am 2026-06-19: `NavigationMap.jsx` und `NavigationScreen.js` zeigen temporaere Map-Debug-Badges, sichtbare Map-Container-Rahmen und loggen `onMapReady`/`onMapLoaded`/Region-Callbacks, um Rendering/Overlay-Probleme auf dem Geraet einzugrenzen. Keine Android-/Google-Konfiguration, keine API-/Location-/Route-/Push-/Heartbeat-/Geofence-Logik geaendert; kein Gradle Build, keine APK-Installation.
- Mobile-Maps-Geraetetest am 2026-06-19: Die Release-APK wurde lokal erfolgreich gebaut, auf dem Smartphone gestartet und die Google-Basemap ist im Tab Karte sichtbar. Ursache war Google Cloud/API-Key-Konfiguration, nicht App-Code: MapView war gemountet, `mapReady` war true, vorherige Logs zeigten Google Maps `Authorization failure`/`INVALID_ARGUMENT`; nach temporaerer Lockerung der Application restrictions auf `None` beim verwendeten Android-Maps-Key wurde die Karte sichtbar. `mobile/.env` enthaelt die lokalen Maps-/Directions-Keys, ist ignored/untracked und Werte werden nicht dokumentiert. Android nutzt weiter den Manifest-Placeholder fuer den Maps-Key; kein hardcoded Key in der Zielkonfiguration. Die temporaere Map-Debug-UI (`Map debug`, `mapReady`, `[map]` Logs) ist noch vorhanden und muss spaeter entfernt werden. `FIS_AUTH_ERROR` bleibt ein separater offener Push-/Firebase-Blocker; Heartbeat/Push/Geofence-Core beim Cleanup nicht gefaehrden. Vor Push/Public/Beta muessen Google Keys rotiert und sauber restriktiert werden: Android app restriction fuer `com.ecily.stepsmatch` mit SHA-1 des installierten Builds sowie API restrictions fuer Maps SDK for Android und Directions API. Kein Push, kein Deploy, keine DB-Mutation.
- Mobile-Directions-/Cleanup-Stand am 2026-06-20: Der lokale Worktree wurde bereinigt; alte Frontend-/Mobile-/Push-Diffs sowie `google-services.json`-Key-Diffs wurden nicht uebernommen. Die sichtbare Map-Debug-UI, Map-Container-Debug-Rahmen und `[map]` Logs wurden aus `NavigationMap.jsx` und `NavigationScreen.js` entfernt. Die lokale `mobile/.env` wurde fuer den Directions-Key korrigiert; kein Key-Wert wurde dokumentiert oder committed. Android-App-ID/Scheme sind auf `com.ecily.stepsmatch`/`stepsmatch` ausgerichtet. `./gradlew.bat assembleRelease --refresh-dependencies` scheiterte an Gradle-Plugin-Aufloesung fuer `org.gradle.toolchains.foojay-resolver-convention`; `./gradlew.bat assembleRelease` war erfolgreich und erzeugte `app-release.apk`. ADB-Installation, Sichtpruefung und Logcat konnten nicht abgeschlossen werden, weil kein ADB-Geraet verbunden war. `FIS_AUTH_ERROR` bleibt separater offener Push-/Firebase-Blocker. Vor Push/Public/Beta muessen Google Keys weiter rotiert und sauber restriktiert werden. Kein Push, kein Deploy, keine DB-Mutation.
- Mobile-Directions-Fix bestaetigt am 2026-06-20: Der richtige Google-Directions-Key wurde lokal in `mobile/.env` gesetzt und nicht dokumentiert. Die Release-APK wurde neu gebaut, auf dem Geraet installiert und die Navigation manuell getestet. Google Maps bleibt sichtbar, die Map-Debug-UI bleibt entfernt und `Google Directions: REQUEST_DENIED` ist am Geraet nicht mehr sichtbar. Der gefilterte Logcat-Check nach dem Testfenster zeigte keine Treffer fuer `REQUEST_DENIED`, `Authorization failure`, `API key` oder `FIS_AUTH_ERROR`. Vor Push/Public/Beta muessen Google Keys rotiert und sauber restriktiert werden. Kein Push, kein Deploy, keine DB-Mutation.
- Mobile-Background-Heartbeat-Geraetetest am 2026-06-20: Auf dem verbundenen Android-Geraet (`com.ecily.stepsmatch`, Version `1.0.1`, versionCode 2) waren Standort, Background-Location, Notifications, Foreground-Service und Internet erteilt. Der native `HeartbeatService` lief als Foreground Service (`stepsmatch-bg-location-task`) und blieb im Screen-off-Test aktiv. Logcat zeigte bei ausgeschaltetem Display `native stale heartbeat tick` und anschliessend `heartbeat sent ok code=200 reason=native-stale`; keine Treffer fuer `REQUEST_DENIED`, `Google Directions`, `Authorization failure`, `FIS_AUTH_ERROR` oder `DeviceNotRegistered` im relevanten Testfenster. Google Maps/Directions blieben ohne sichtbaren Key-Fehler; die Map-Debug-UI bleibt entfernt. Treffer-/Push-Ausloesung wurde ohne DB/API-Mutation nicht kuenstlich erzwungen. Ein fremder Braze-SDK-Log enthielt nur den Text `api key`, keinen StepsMatch-/Google-Fehler und keine dokumentierten Werte. Vor Push/Public/Beta muessen Google Keys rotiert und sauber restriktiert werden. Kein Push, kein Deploy, keine DB-Mutation.
- Mobile-Background-Push-Hit-E2E am 2026-06-20: Der vollstaendige E2E-Test ist gelb, nicht gruen. Testsetup: Android-Geraet `com.ecily.stepsmatch` mit erteilten Standort-/Background-/Notification-Rechten, Screen-off-Heartbeat, Live-Backend/Mongo. Kontrollierte Testdaten wurden mit Marker `E2E_TEST_STEPSMATCH` in `offers` angelegt: zuerst ein nicht matchendes Marker-Offer, danach ein Match-Offer mit Kategorie/Interesse `restaurant`, `validDays=['Samstag']`, `validTimes=00:00-23:59`, Radius 250 m an der letzten bekannten Testgeraet-Position. Der Screen-off-Heartbeat kam am Backend an; `PushToken.lastHeartbeatAt` wurde im Testfenster aktualisiert. Backend-Matching erkannte das Match-Offer als `inside` und schrieb `OfferVisibility` (`status=seen`, `inside=true`). Ein sichtbarer StepsMatch-Push am Handy erschien, war aber nicht eindeutig dem markierten Testoffer zuzuordnen (`Testangebot zuhause`, kein Marker). Backend-Remote-Push fuer das markierte Offer blieb blockiert: `POST /api/offers/:id/notify-now` fand 1 passenden Token (`total=1`, `tried=1`), aber `sent=0`, `skipped=1`, keine `DeviceNotRegistered`-Receipts. Lokaler Utility-Gegentest mit `sendPushAndCheckReceipts` zeigte als konkrete Ursache `unable to verify the first certificate` beim Request zu `https://exp.host/--/api/v2/push/send`; Token, Project-ID und DeviceNotRegistered waren nicht der Blocker. Cleanup abgeschlossen: beide markierten Testoffers und die zugehoerige `OfferVisibility` wurden geloescht; keine markierten `E2E_TEST_STEPSMATCH`-Offers blieben uebrig. Keine Secrets/Tokens dokumentiert, kein Deploy, kein Push.
- Backend-Expo-Push-TLS-/FCM-E2E am 2026-06-20: TLS/CA wurde lokal eingegrenzt. Unter Windows/Node `v22.22.0` scheitert Nodes gebuendelter CA-Store gegen `https://exp.host/--/api/v2/push/send` mit `UNABLE_TO_VERIFY_LEAF_SIGNATURE`; derselbe Check ist mit `node --use-system-ca` erfolgreich (`authorized=true`, HTTP 405 bei GET erwartbar). Es wurde keine unsichere TLS-Abschaltung verwendet, insbesondere kein `NODE_TLS_REJECT_UNAUTHORIZED=0` und kein `rejectUnauthorized:false`. MongoDB-Verbindung und lokaler `sendPushAndCheckReceipts`-Utility-Test funktionieren mit `--use-system-ca` bis zum Expo-Gateway, danach liefert Expo fuer den Android-Push ein Ticket `InvalidCredentials` (`Unable to retrieve the FCM server key...`). Laut Expo-Doku deutet `InvalidCredentials` auf fehlende/ungueltige Android-FCM-V1-Push-Credentials im Expo/EAS-Projekt beziehungsweise nicht passende Firebase-Credentials hin. Der erneute kontrollierte E2E nutzte ein neues markiertes Offer `E2E_TEST_STEPSMATCH` mit gueltigem `validDays=['Samstag']`, `validTimes=00:00-23:59`, Kategorie/Interesse `restaurant`, Radius 300 m an der letzten bekannten Testgeraet-Position. `POST /api/offers/:id/notify-now` auf dem Live-Backend fand 1 passenden Token (`total=1`, `tried=1`), blieb aber bei `sent=0`, `skipped=1`. Im Screen-off-Test kam der Heartbeat erneut an (`PushToken.lastHeartbeatAt` aktualisiert), Matching schrieb `OfferVisibility` als `status=seen`, `inside=true`; eine markierte `E2E_TEST_STEPSMATCH`-Push-Notification kam nicht am Handy an. Kein `FIS_AUTH_ERROR`, kein `DeviceNotRegistered`, kein Google-Directions-/Maps-Key-Fehler im relevanten Fenster. Cleanup abgeschlossen: 1 markierter Provider, 1 markiertes Offer und 1 zugehoerige `OfferVisibility` wurden geloescht; keine markierten `E2E_TEST_STEPSMATCH`-Testdaten blieben uebrig. Offener Blocker ist jetzt Android/Expo FCM-V1-Credentials, nicht mehr Directions/Maps und lokal nicht mehr TLS. Fuer lokale Backend-Push-Smokes auf Windows `NODE_OPTIONS=--use-system-ca` oder `node --use-system-ca` verwenden; fuer DigitalOcean erst pruefen/setzen, falls dort ebenfalls ein CA-Fehler in Runtime-Logs sichtbar ist. Keine Secrets/Tokens dokumentiert, kein Deploy, kein Push.
- Mobile-EAS-Slug-Fix am 2026-06-20: `npx eas-cli credentials -p android` war durch einen Slug-Mismatch blockiert: Die `extra.eas.projectId` gehoert laut EAS-Fehler zum Expo-Projekt-Slug `mobile`, lokal standen `mobile/app.json` und `mobile/app.config.js` aber auf `stepsmatch-mobile`. Beide lokalen Expo-Config-Slugs wurden auf `mobile` ausgerichtet; `extra.eas.projectId` bleibt `08559a29-b307-47e9-a130-d3b31f73b4ed`, Android Package/Application-ID bleibt `com.ecily.stepsmatch`, Owner ist lokal nicht gesetzt und wird ueber den angemeldeten EAS-Account/Projektkontext aufgeloest. Ziel ist, die Android-FCM-V1-Credentials im EAS-Menue pruefen zu koennen. Keine Secrets dokumentiert, keine Credential-Dateien geaendert, kein Backend/Frontend, keine DB, kein Push.
- Android-FCM-V1-Push-Retest am 2026-06-20: Der Google Service Account Key wurde laut Betreiber in EAS erfolgreich fuer `com.ecily.stepsmatch` hochgeladen/zugewiesen; keine Credential-Datei wurde ins Repo kopiert oder dokumentiert. Der lokale Backend-Utility-Test `sendPushAndCheckReceipts` gegen Expo Push war danach gruen: 1 Ticket `ok`, Receipt-Summary `ok=1`, kein `InvalidCredentials`, kein TLS-Fehler, kein `DeviceNotRegistered`. Kontrollierte Testdaten mit Marker `E2E_TEST_STEPSMATCH` wurden angelegt: 1 Provider und 3 Offers (`NOTIFY_NOW`, `SCREEN_OFF`, `SCREEN_OFF_BG`) mit Kategorie/Interesse `restaurant`, gueltigem `validDays=['Samstag']` und `validTimes=00:00-23:59`. Ein kontrollierter Heartbeat/Matching-Lauf schrieb fuer die markierten Offers `OfferVisibility` als `notified`; am Geraet waren eindeutige FCM-Notifications mit Titeln `E2E_TEST_STEPSMATCH FCM`, `E2E_TEST_STEPSMATCH NOTIFY_NOW ...`, `E2E_TEST_STEPSMATCH SCREEN_OFF ...` und `E2E_TEST_STEPSMATCH SCREEN_OFF_BG ...` sichtbar. Der echte 5-Minuten-Screen-off-Abschnitt war trotzdem nicht voll gruen, weil kein neuer nativer Screen-off-Heartbeat in `PushToken.lastHeartbeatAt` ankam; der StepsMatch-Foreground-Service war im Testfenster nicht aktiv, und ein ADB-Start des nicht exportierten `HeartbeatService` ist kein valider App-E2E-Ersatz. Keine Treffer fuer `InvalidCredentials`, `DeviceNotRegistered` oder `FIS_AUTH_ERROR` im relevanten Fenster. Cleanup abgeschlossen: 3 markierte Offers, 1 markierter Provider und 3 zugehoerige `OfferVisibility` wurden geloescht; `remainingMarkedOffers=0`, `remainingTestVis=0`. Offener Punkt: App-seitigen Start/Erhalt des nativen Background-Heartbeat-Service erneut klaeren, bevor der komplette Screen-off-Hit-Push-E2E als gruen gilt.
- Mobile-Native-Heartbeat-Fix am 2026-06-20: Ursache fuer den gelben Screen-off-Test war der App-seitige Startpfad des nativen `HeartbeatService`: Der Service wurde nur ueber JS-Config-Sync angestossen und beim Hintergrundwechsel nicht erneut abgesichert; zudem konnte ein stationaeres Geraet ohne neue Location-Updates ohne initiale native Position bleiben. Fix: Android synchronisiert die native Heartbeat-Konfiguration jetzt beim Background-Location-Start und beim AppState-Wechsel nach `background`/`inactive`; der native Service seeded beim Start die letzte bekannte Position und sendet damit bei verfuegbarer Position einen initialen `native-start`-Heartbeat. Release-APK wurde erfolgreich gebaut und auf dem verbundenen Geraet installiert. Permissions fuer Location, Background-Location, Notifications, Foreground-Service und Internet waren erteilt. Im Screen-off-Test blieb `HeartbeatService` als Foreground-Service aktiv (`foregroundId=1001`) und Logcat zeigte `native stale heartbeat tick` gefolgt von `heartbeat sent ok code=200 reason=native-stale`; damit hat das Backend im Screen-off-Fenster einen nativen Heartbeat mit HTTP 200 bestaetigt. Keine Treffer fuer `FIS_AUTH_ERROR`, `DeviceNotRegistered`, Google Directions `REQUEST_DENIED` oder Maps `Authorization failure` im relevanten StepsMatch-Filter. Es wurden keine Testdaten angelegt und kein voller Treffer-/Push-E2E wiederholt; naechster Schritt ist ein erneuter markierter E2E mit `E2E_TEST_STEPSMATCH`. Keine Secrets/Tokens/Keys dokumentiert, kein Backend/Frontend, keine DB-Mutation, kein Push, kein Deploy.
- Finaler Background-Push-E2E am 2026-06-20: Vollstaendiger Screen-off Treffer-/Push-Test ist gruen. Setup: installiertes Android `com.ecily.stepsmatch` Version `1.0.1`/versionCode 2 mit Heartbeat-Fix `76ca0d9`, erteilten Location-, Background-Location-, Notification-, Foreground-Service- und Internet-Rechten; Live-Backend/Mongo; lokaler Mongo-Zugriff unter Windows mit sicherem `node --use-system-ca` wegen bekanntem CA-Store-Thema. Kontrollierte Testdaten wurden direkt in Mongo mit Marker `E2E_TEST_STEPSMATCH` angelegt: 2 Provider und 2 Offers; der finale Nachweis nutzte `E2E_TEST_STEPSMATCH SCREENOFF_FINAL 20260620114820` mit Kategorie/Interesse `restaurant`, Radius 350 m, gueltigem `validDays=['Samstag','Saturday',6]`, `validTimes=00:00-23:59` und Standort an der letzten bekannten Testgeraet-Position. Vor dem finalen Screen-off wurde die Visibility des finalen Offers gezielt entfernt (`visibilityCount=0`). Im Screen-off-Fenster zeigte Logcat zweimal `native stale heartbeat tick` und `heartbeat sent ok code=200 reason=native-stale`; Mongo bestaetigte `PushToken.lastHeartbeatAt=2026-06-20T11:54:40.048Z`. Backend-Matching schrieb fuer den finalen Offer `OfferVisibility` mit `status=notified`, `inside=true`, `lastReason=inside`, Distanz 0 m und `lastNotifiedAt=2026-06-20T11:50:40.220Z`. Auf dem Geraet war eine eindeutige FCM-Notification im Channel `offers-v2` mit Titel `E2E_TEST_STEPSMATCH SCREENOFF_FINAL 20260620114820` sichtbar. Keine Treffer fuer `InvalidCredentials`, `DeviceNotRegistered`, `FIS_AUTH_ERROR`, Google Directions `REQUEST_DENIED` oder Maps `Authorization failure` im relevanten Testfenster. Cleanup abgeschlossen: 2 markierte Offers, 2 markierte Provider und 2 zugehoerige `OfferVisibility` wurden geloescht; `remainingMarkedOffers=0`, `remainingMarkedProviders=0`, `remainingTestVis=0`. Keine Secrets/Tokens/Keys dokumentiert, kein Backend-/Frontend-Deploy, kein Push.
- Frontend-Beta-Vorbereitung am 2026-06-20: `stepsmatch.com` wurde kommunikativ und strukturell auf PRE-ALPHA/Beta-Vorbereitung ausgerichtet. Die oeffentliche Landingpage nutzt jetzt die Kernbotschaft `PRE ALPHA · Raum Graz im Aufbau`, `Angebote finden dich.` und erklaert einfach, dass StepsMatch ruhig im Hintergrund laeuft und nur meldet, wenn in der Naehe etwas passt. Neue/oeffentliche Marketingrouten wurden ergaenzt: `/app`, `/anbieter`, `/so-funktionierts`, `/pre-alpha`, `/datenschutz-standort`; `/` ist nun oeffentliche Landing, `/tester` bleibt Tester-Gate, geschuetzte App-/Adminpfade bleiben hinter dem Gate. Anbieter-Copy vermeidet Garantien, Dealportal-Sprache und falsche Partner-Claims; Demo-/Spot-/Ortshinweise werden klar als PRE-ALPHA-Datenstrategie beschrieben. Fuer SPA-Direktaufrufe wurde `frontend/public/_redirects` mit `/* /index.html 200` ergaenzt; lokaler Preview-Smoke lieferte HTTP 200 fuer `/`, `/app`, `/anbieter`, `/so-funktionierts`, `/pre-alpha`, `/datenschutz-standort`, `/impressum` und `/_redirects`, und `dist/_redirects` wurde erzeugt. Checks: `npm run lint` und `npm run build` im Frontend erfolgreich. Push/Auto-Deploy lief mit Commit `797fac4`; live unter `https://www.stepsmatch.com/` wurde das neue JS-Asset mit PRE-ALPHA-/Raum-Graz-Copy ausgeliefert. Live-Server-Direktaufrufe wie `/app`, `/anbieter` und `/impressum` liefern aber weiterhin HTTP 404, obwohl `/_redirects` ausgeliefert wird; Browser-Fallback ueber bestehende `404.html`/`/?redirect=...` funktioniert als Client-Redirect, echte serverseitige SPA-Rewrite-Konfiguration bleibt ein offener Hosting-Punkt. Keine Backend-/Mobile-/APK-Aenderung, keine DB-Mutation, keine Secrets/Credentials.
- MVP-Neustart-DB-Cleanup am 2026-06-20: Fuer einen sauberen manuellen Field-E2E-Neustart wurden kontrolliert nur fachliche Alt- und Runtime-Daten per `deleteMany({})` geleert, kein `dropDatabase()`: `users` 8->0, `providers` 107->0, `offers` 101->0, `offervisibility` 2->0, `pushtokens` 3->0, `clientdiaglogs` 1094->0, `notificationlogs` 0->0, `diagnostics` 0->0, `offer_poller_pushlocks` 0->0. Bewusst behalten wurden technische/global benoetigte Collections: `categories` 12, `subcategories` 48, `testers` 1 und `offer_poller_leader` 1. Verifikation: `Provider.location`, `Offer.location` und `PushToken.lastLocation` behalten ihre `2dsphere`-Indizes; `users` ist leer, daher blockieren bestehende Unique-Indizes auf `email`/`username` keine Neuregistrierung; Push-/Heartbeat-Runtime ist durch `pushtokens=0` zurueckgesetzt. Direkter DO-Backend-Healthcheck `https://lobster-app-ie9a5.ondigitalocean.app/api/health` lieferte HTTP 200; `https://www.stepsmatch.com/api/health` bleibt HTTP 404 und ist damit weiter ein Domain-/Proxy-Routing-Punkt. Keine Dokumentinhalte, Tokens, E-Mails oder Secrets wurden ausgegeben oder dokumentiert; kein Deploy, kein APK-Build, keine Credential-Aenderung.
- Frontend-Anbieter-Einstieg am 2026-06-20: `/anbieter` zeigte zuvor oberhalb des Folds nur Anbieter-Marketingcopy; der praktische Login-/Registrierungsweg war fuer den MVP-Field-Test nicht sofort auffindbar. Frontend-Fixes `8fe3a84` und `193a6e2`: Die Anbieter-Seite zeigt im oberen CTA-Bereich jetzt `Anbieter registrieren` -> `/register` und `Anbieter einloggen` -> `/login`; der Header-Button `Anbieter werden` fuehrt direkt zu `/register`, waehrend die Navigation `Anbieter` weiter `/anbieter` oeffnet. Nach Rebase auf Remote-Commit `ff4a7e5` wurde die dortige Direkt-Route `/anbieter` -> Register wieder entfernt, damit `/anbieter` wie gefordert die Anbieter-Seite mit sofort sichtbaren Auth-CTAs rendert. Bestehende Anbieter-Erklaerung bleibt erhalten, keine geschuetzten Dashboard-/Admin-Routen wurden oeffentlich gemacht. Lokale Checks: `npm run lint`, `npm run build`, Vite-Preview-Smoke fuer `/anbieter`, `/register`, `/login` jeweils HTTP 200; Build-Chunks enthalten die neuen Button-Texte und Linkziele. Push/Auto-Deploy lief mit Commit `6d07c7a`; live wird Root-Asset `assets/index-BCe1bfnU.js` ausgeliefert, `MarketingInfoPage-DieOsh5H.js` enthaelt `Anbieter registrieren`, `Anbieter einloggen`, `/register` und `/login`, und `Navbar-J_-RIm1w.js` enthaelt `/register`. Direkte Serveraufrufe `https://www.stepsmatch.com/anbieter`, `/register` und `/login` liefern weiterhin HTTP 404; Browser-Fallback ueber `404.html`/`/?redirect=...` bleibt der bekannte Client-Redirect-Workaround, echte serverseitige SPA-Rewrite-Konfiguration bleibt offen. Keine Backend-/Mobile-/APK-Aenderung, keine DB-Mutation, keine Secrets/Credentials.
- Frontend-SPA-Rewrite-Pruefung am 2026-06-21: Im Repo gibt es keine `.do/`, `.digitalocean/`, `app.yaml` oder `static.yaml`; dokumentiert ist nur DigitalOcean App Platform Static Site mit Working Directory `frontend`, Build `npm ci && npm run build`, Output `dist`. `frontend/public/_redirects` wird live als Textdatei ausgeliefert, aber von DigitalOcean nicht als Rewrite interpretiert. Laut DigitalOcean-App-Platform-Doku muss fuer Static Sites die Custom Page `Catchall` auf `index.html` gesetzt werden bzw. in einer angewendeten App Spec `static_sites[].catchall_document: index.html`. Ohne aktuellen DO-App-Spec/Token ist ein repo-seitiger Fix nicht sicher moeglich, weil eine neue unvollstaendige Spec bestehende Komponenten/Env/Domains ueberschreiben koennte und ein Spec-File im Repo nicht automatisch angewendet wird. Live-Status bleibt: `/anbieter`, `/register`, `/login` direkte Requests HTTP 404; Dashboard-Handlungsbedarf: Frontend-Static-Site -> Settings -> Custom Pages -> Catchall -> `index.html`, danach Redeploy/Live-Smoke. Keine Backend-/Mobile-/APK-Aenderung, keine DB-Mutation, keine Secrets/Credentials.
- Frontend-Angebot-Erstellung am 2026-06-21: `frontend/src/components/AddOfferForm.jsx` wurde mit Commit `168e315` um dieselbe `validDays`-Wochentagsauswahl wie `EditOfferForm` ergaenzt (`Mo`, `Tu`, `We`, `Th`, `Fr`, `Sa`, `Su`). Initial bleiben alle Tage aktiv; der Payload nutzt weiter `validDays` mit `Monday` bis `Sunday`, passend zur bestehenden Backend-Normalisierung. Checks: `npm run lint`, `npm run build`, lokaler Vite-Preview-Smoke fuer `/add-offer/smoke-provider-id` und `/edit-offer/smoke-offer-id` jeweils HTTP 200; kein Submit, keine DB-Mutation. Push nach `origin/main` abgeschlossen (`168e315`, Doku `63d2a05`, DO-Checkliste `12b7d1a`). Auto-Deploy lieferte danach Root-Asset `assets/index-CXaa2XiH.js`; `assets/AddOfferForm-BHxlNIYk.js` enthaelt `validDays`, alle Wochentagswerte und die neue Toggle-Button-Logik. Direkter Live-Aufruf `/add-offer/smoke-provider-id` bleibt wegen fehlendem DO-Catchall HTTP 404; `/?redirect=/add-offer/smoke-provider-id` liefert HTTP 200 als bestehender Client-Redirect-Workaround. Keine Backend-/Mobile-/APK-Aenderung, keine Secrets/Credentials.
- MVP-Field-Test am 2026-06-21: Web-Anbieterflow ist gruen; DigitalOcean-Catchall fuer SPA-Direktrouten wurde gesetzt/deployed; `/add-offer` validDays ist live. Testbestand nach Produktflow: 2 Anbieter und 4 Offers, darunter das neue echte Offer `MVP E2E Test`; keine DB-Mutation ausser echter User-/Offer-Anlage ueber Produktflow. Ein neuer App-User wurde angelegt; Permissions sind gruen; der native HeartbeatService laeuft. PushToken deviceId `5060f7c4...` ist `valid=true`, hat Standort und `interests=['restaurant','bar']`; Offers laden korrekt. Fuer `MVP E2E Test` wurde OfferVisibility fuer diesen Token angelegt: `status=notified`, `inside=true`, Distanz ca. 151 m, `lastReason=inside`; `LOCAL_PUSH_SHOWN:INSTANT_NEW_OFFER` belegt den lokalen Sofort-Push. Map/Directions sind gruen. Im Testfenster keine `DeviceNotRegistered`, `InvalidCredentials`, `FIS_AUTH_ERROR` oder `REQUEST_DENIED`. Hinweis: `User.interests` blieb leer, `PushToken.interests` ist korrekt; spaeter haerten. Remote-/FCM-Screen-off wurde fuer dieses konkrete neue Web-Offer nicht separat wiederholt; der fruehere MVP-Anker fuer Background-Heartbeat/Screen-off bleibt gueltig. Keine Secrets und keine vollstaendigen Tokens dokumentiert.
- Backend-Feed-Fix am 2026-06-21: `MVP E2E Test` war fachlich aktiv/gueltig/im Radius und in OfferVisibility `notified/inside`, fehlte aber im App-Home-Feed. Root Cause war der Mongo-Prefilter fuer `/api/offers?activeNow=1`: `validDates.to >= now` schloss date-only gespeicherte Enddaten wie `2026-06-21T00:00:00.000Z` tagsueber zu frueh aus, bevor die finale ActiveNow-Logik greifen konnte. Fix `3a7fcac`: Der ActiveNow-Prefilter nutzt jetzt das lokale Tagesfenster und behandelt date-only `validDates.to` bis Tagesende als Kandidat; finale Logik fuer `validDates`, `validDays` und `validTimes` bleibt massgeblich. Checks: `node --test backend/tests/activeNow.test.mjs`, `node --check backend/routes/offers.js`, `node --check backend/utils/activeDatesPrefilter.js`, `npm run test --if-present` in `backend`. Live-API-Smoke nach Auto-Deploy: `/api/offers` mit `activeNow=1`, Interessen `restaurant,bar` und Standortfilter liefert 4 Datensaetze inkl. `MVP E2E Test`; App-Home-Feed zeigt `MVP E2E Test` nach Refresh sichtbar im Startscreen. Keine DB-Mutation, keine Mobile-/Frontend-Aenderung, keine Secrets.
- Mobile-Notification-Field-Learning am 2026-06-23: Technisch korrekte StepsMatch-Benachrichtigungen koennen auf Android in Hose/Tasche uebersehen werden, wenn Vibration und Ton zu dezent sind. StepsMatch nutzt fuer echte relevante Naehe-/Match-Hinweise jetzt den neuen versionierten Attention-Channel `stepsmatch-nearby-attention-v1` mit MAX-Importance, Default-Sound, aktivierter Vibration und Pattern `[0, 500, 180, 500, 180, 900]`. Nicht alle Notifications werden maximal laut gemacht: Diagnose, Roundtrip, Heartbeat-/Service-Status und generische Tests bleiben auf Default-/Legacy-Kanaelen, ausser dem expliziten Diagnostics-Test `Test strong nearby notification`. Ultreia-Learning, ohne Code-Vermischung: Pilger schauen unterwegs nicht staendig aufs Handy; spaetere Ultreia-Implementierung sollte getrennte Channel-Klassen wie `critical_need`, `normal_need` und `discovery_silent` pruefen und separat im Ultreia-Projekt umsetzen.
- Mobile-Notification-APK-Teiltest am 2026-06-23: Commit `41bb03f` wurde lokal mit `mobile/android/gradlew.bat assembleRelease` erfolgreich gebaut und `app-release.apk` auf das verbundene Android-Geraet `V4RKTOIF6TBAIR5L` als `com.ecily.stepsmatch` installiert. Notifications waren nach `adb pm grant`/`appops` auf App-Ebene erlaubt (`importance=DEFAULT`); `dumpsys notification` zeigte danach die Channels `stepsmatch-bg-location-task`, `stepsmatch-default-v2` und `stepsmatch-nearby-attention-v1`. Der neue Nearby-Channel hatte Sound, Licht, Vibration und Pattern `[0, 500, 180, 500, 180, 900]`; Android meldete fuer ihn `mImportance=5` (geraeteseitige HIGH/MAX-nahe Darstellung). Der Diagnostics-Button `Test strong nearby notification` konnte nicht sauber ausgeloest werden, weil der Tab/Deep-Link `diagnostics` im installierten Build auf `Route nicht gefunden` fuer `/(tabs)/diagnostics` lief. Danach wurde der Strong-Notification-Test ohne Backend-/DB-Pfad im sicher erreichbaren Profil-Tab als interner Button `Starke Nearby-Notification testen` verfuegbar gemacht; er plant lokal nach 3 Sekunden `stepsmatch-nearby-attention-v1` und loggt `[notify] strongNearbyTest channel=stepsmatch-nearby-attention-v1`. Kein Remote-/Backend-Push wurde erzwungen; kein neuer Offer-/DB-Testdatensatz wurde angelegt. Naechster Testschritt bleibt ein APK-/Device-Test mit Screen-off/Hosentasche fuer Sound, Vibration und Heads-up. Ultreia-Learning bleibt nur als Learning markiert, ohne Code- oder Produktvermischung.
- Mobile-Strong-Notification-Profiltest am 2026-06-23: Commit `17928b4` wurde lokal erfolgreich als Release-APK gebaut und auf `V4RKTOIF6TBAIR5L` installiert. Nach Neuinstallation war der Profil-Tab erst nach Permission-Gate erreichbar; zur Einhaltung der No-DB-Mutation-Grenze wurden WLAN/Mobile Data fuer den Gate-Durchlauf temporaer deaktiviert und danach wieder aktiviert. Der Profilpfad `stepsmatch:///ProfileScreen` war erreichbar, der Abschnitt `Interner Test` und der Button `Starke Nearby-Notification testen` waren sichtbar; der Button war im UI-Dump clickable/focusable. Automatisierte ADB-Ausloesung per Koordinaten-Tap, Long-Touch, TAB+ENTER und TAB+DPAD_CENTER erzeugte jedoch keinen Logcat-Marker `[notify] strongNearbyTest...` und keine sichtbare Test-Notification. Screen-on- und Screen-off/Hosentasche-Wahrnehmung bleiben dadurch unbestaetigt. `dumpsys notification` bestaetigte weiter `stepsmatch-nearby-attention-v1` mit `mImportance=5`, Default-Sound, Licht und Vibration `[0, 500, 180, 500, 180, 900]`. Wahrnehmbarkeit bleibt unklar; Nutzer-Channel-Settings, DND/Lautlos, OEM-Verhalten und die tatsaechliche manuelle Touch-Ausloesung muessen im naechsten Device-Test geprueft werden. Ultreia-Learning nur getrennt: starke Attention-Channels sind fuer Pilger relevant und spaeter separat in einer Ultreia-Push-Reliability-ADR zu behandeln, ohne Code-/Projektvermischung.
- Mobile-Strong-Notification-Instrumentierung am 2026-06-23: Der Profil-Button `Starke Nearby-Notification testen` loggt jetzt eindeutig `[notify] strongNearbyTest buttonPressed`, `permissionStatus=...`, `scheduling channel=stepsmatch-nearby-attention-v1 trigger=3s`, `scheduled ok` oder `error=...`. Direkt unter dem Button erscheint sichtbares Feedback, zuerst `Test wird in 3 Sekunden ausgelöst.`, bei fehlender Notification-Permission eine klare Meldung und bei Fehlern der Fehlertext. Zweck: Im naechsten APK-/Device-Test per manuellem Finger-Tap und Logcat-Filter `strongNearbyTest` sauber unterscheiden, ob der Button-Handler nicht feuert oder ob das Notification-Scheduling stoppt. Keine Backend-/DB-/Remote-Push-Aenderung; Ultreia bleibt nur getrenntes Learning.
- Im Repo vorhandene Ultreia-/Camino-Texte werden als aktueller repo-interner Stand dokumentiert, nicht als Fremdprojektkontext.

## 2. Aktueller technischer Stack

- Mobile: Expo SDK 53, React Native 0.79.5, React 19, Expo Router, Android native Gradle-Projekt, Kotlin Native-Heartbeat-Service.
- Frontend/Web: React 19, Vite 6, React Router 7, Tailwind CSS, axios, lucide-react, motion, Google Maps API Komponenten.
- Backend: Node.js ESM, Express 4, Mongoose 7, MongoDB, helmet, cors, compression, morgan.
- Datenbank: MongoDB mit Mongoose-Modellen und 2dsphere-Indizes fuer Angebote, Provider und PushToken-Standorte.
- Push/Notifications: Expo Push Service, `expo-notifications`, Android Notification Channels, serverseitige Expo Push Receipts, lokale Geofence-Notifications.
- Auth: Backend nutzt bcrypt und JWT; User-Registrierung hat E-Mail-Verifikation mit optionalem Resend-Provider. Frontend/Mobile speichern Auth-State lokal.
- Hosting/Deployment: Dokumentiert ist DigitalOcean App Platform fuer Backend/Frontend und DigitalOcean Spaces fuer APK-Download via `/apk` Redirect.
- Sonstige Services: Google Maps/Directions, Cloudinary Uploads, Firebase/Google Services fuer Android-Konfiguration, optional Resend fuer E-Mail.

## 3. Projektstruktur

- `.github/workflows/project-analysis.yml`: manueller Analyse-Workflow, nutzt `secrets.CLONE_URL` und erzeugt ein Struktur-Artefakt.
- `backend/`: Express API, MongoDB-Konfiguration, Modelle, Routen, Jobs, Scripts.
- `frontend/`: Vite/React Web-Frontend fuer Landingpages, Anbieter-Login/Register, Provider/Offer-Dashboard und Admin-Ansichten.
- `mobile/`: Expo/React-Native-App mit file-based routing, Android native Projekt, Push-/Location-Komponenten und Assets.
- `docs/`: Architektur- und Release-Dokumentation sowie lokale Release-Smoke-Skripte.
- Root-Dokumente: `README.md` ist minimal, `CONTEXT.md` enthaelt umfangreiche historische Arbeitsnotizen, `DEPLOY_DO_CHECKLIST.md` enthaelt DO-Deploy-Hinweise mit noch sichtbaren Ultreia-Bezeichnungen.
- Build-/Deployment-Dateien: `backend/package.json`, `frontend/package.json`, `frontend/vite.config.js`, `mobile/package.json`, `mobile/app.config.js`, `mobile/app.json`, `mobile/eas.json`, `mobile/android/*`, `.github/workflows/project-analysis.yml`.
- Keine Docker-Dateien wurden gefunden.

## 4. Mobile App

- Pfad: `mobile/`
- Framework/SDK: Expo SDK `~53.0.20`, React Native `0.79.5`, Expo Router `~5.1.4`.
- Paketname: Expo/Gradle `applicationId` aktuell `com.ecily.stepsmatch`; Android `namespace` ist `com.ecily.mobile`. Aeltere Doku nennt noch `com.ecily.mobile`, das ist ein Risiko/Abgleichpunkt.
- App-Version: `1.0.1`, `versionCode` 2.
- Scheme: `stepsmatch`; Android Intent-Filter auch fuer `exp+stepsmatch`.
- Build-Prozess:
  - `npm run start`
  - `npm run android`
  - `npm run ios`
  - `npm run web`
  - `npm run lint`
  - EAS-Profile `development`, `preview`, `production`, jeweils Android APK.
  - Native Release laut Doku: `cd mobile/android && ./gradlew.bat assembleRelease`
- relevante Berechtigungen:
  - `ACCESS_COARSE_LOCATION`
  - `ACCESS_FINE_LOCATION`
  - `ACCESS_BACKGROUND_LOCATION`
  - `FOREGROUND_SERVICE`
  - `FOREGROUND_SERVICE_LOCATION`
  - `POST_NOTIFICATIONS`
  - `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`
  - `WAKE_LOCK`
  - `INTERNET`
  - Im generierten AndroidManifest stehen zusaetzlich u. a. `RECEIVE_BOOT_COMPLETED`, `ACCESS_NETWORK_STATE`, `READ/WRITE_EXTERNAL_STORAGE`, `RECORD_AUDIO`, `SYSTEM_ALERT_WINDOW`; Notwendigkeit pruefen.
- bekannte funktionierende Features nach Codeanalyse:
  - Auth-Gate mit Login, Registrierung und E-Mail-Verifikation.
  - Onboarding fuer Interessen und Permissions.
  - Home/Feed laedt aktive Angebote ueber `/offers` mit Standort-/Interessenfilterung und lokaler Distanzlogik.
  - Kartenansicht mit Google Maps, aktiven/allgemeinen Angeboten, Markern, Recenter und Route-Start.
  - Angebotsdetail und Navigation mit Google Directions Polyline-Fetch.
  - Push-Initialisierung, Token-Registrierung, Foreground-Unterdrueckung, lokale Benachrichtigungssignale.
  - Background Location, Geofencing, BackgroundFetch Watchdog, Headless Bootstrap und native Android Heartbeat-Service-Komponente.
  - Profil mit Privacy-Opt-in und Hintergrunddienst-Stopp/Logout-Stop.
  - Diagnostics-Screen fuer Permissions, Channels, Background Tasks, Heartbeats, Logs, Roundtrip und lokale Tests.
- bekannte offene Punkte/Risiken:
  - Brand-/Domain-Drift: Mobile und Frontend zeigen aktuell vielerorts `Ultreia.app`/Camino, waehrend Repo/Ziel StepsMatch.com heisst.
  - Paketname-Divergenz zwischen alter Doku (`com.ecily.mobile`) und aktuellem Gradle/Expo (`com.ecily.stepsmatch`).
  - AndroidManifest enthaelt eine konkrete Google Maps API-Key-Metadatenzeile; Wert nicht dokumentieren, aber Secret-/Key-Handling pruefen.
  - Background-Location/Push-Zuverlaessigkeit bleibt geraete- und OEM-abhaengig.
  - `mobile/README.md` ist noch Expo-Standardtext und nicht projektaktuell.
  - Viele Mobile-Dateien sind uncommitted; Status ist nicht als stabile Baseline gesichert.

## 5. Backend

- Pfad: `backend/`
- Stack: Node.js `>=18 <23`, Express, Mongoose, MongoDB, Expo Server SDK, Cloudinary, JWT, bcrypt.
- Start-/Build-Befehle:
  - `npm run start` -> `node server.js`
  - `npm run dev` -> `nodemon server.js`
  - `npm run build-indexes` -> `node ./scripts/create-indexes.js`
  - `npm run seed:graz`
  - `npm run seed:reset`
  - `npm run seed:gratwein`
- Haupt-Entry-Point: `backend/server.js`
- relevante API-Endpunkte:
  - Health: `GET /api/health`, `GET /api/ping`, `GET /api/_healthz`, `GET /api/_readyz`
  - APK: `ALL /apk`, `ALL /api/apk`
  - Users/Auth: `POST /api/users/register`, `POST /api/users/verify-email`, `POST /api/users/resend-verification`, `POST /api/users/login`, `GET /api/users/me`, `POST /api/users/push-token/:userId`, `POST /api/users/test-push/:userId`, `PUT /api/users/preferences/:userId`
  - Providers: `GET/POST /api/providers`, `GET /api/providers/user/:userId`, `GET/PATCH/PUT /api/providers/:id`
  - Offers: `GET /api/offers`, `POST /api/offers`, `POST /api/offers/nearby`, `POST /api/offers/nearby-noauth`, `GET /api/offers/nearby-geofence`, `GET /api/offers/provider/:providerId`, `GET/PUT/DELETE /api/offers/:id`, `POST /api/offers/:id/notify-now`, `POST /api/offers/found/:id`
  - Categories: `GET/POST /api/categories`, `GET /api/categories/subcategories`, `POST /api/categories/:id/subcategories`, `PUT/DELETE /api/categories/:id`
  - Location: `POST /api/location/heartbeat`, `POST /api/location/geofence-enter`, `GET /api/location/ping`
  - Push: `GET/POST /api/push/canary`, `POST /api/push/register`, `POST /api/push/service-state`, `POST /api/push/roundtrip`, `POST /api/push/test`, `POST /api/push/ping`, `POST /api/push/roundtrip-diagnose`
  - Match: `POST /api/match/check`
  - Testers: `POST /api/testers/validate`, `POST /api/testers/accept`
  - Uploads: `GET /api/uploads/_debug`, `POST /api/uploads`, `POST /api/uploads/images`, `DELETE /api/uploads`, `POST /api/uploads/delete`
  - Diagnostics: `POST /api/diag/log`, `GET /api/diag/recent`, `GET /api/diag/heartbeat`, `GET /api/diag/heartbeat-list`
  - Notifications: `POST /api/notifications/action`
- Datenmodelle:
  - `User`: name/firstName/lastName/username/email/password, Email-Verifikationsfelder, preferredRadius, interests, expoPushToken.
  - `Provider`: name/address/categoryId/category/subcategory/description/contact/openingHours/location/user, 2dsphere location index.
  - `Offer`: provider, categoryId/subcategoryId, legacy category/subcategory, name, description, radius, interestsRequired, validDays/weekdays, validTimes, validDates, contact, images, location, languages, foundCounter; 2dsphere und weitere Indizes.
  - `Category`: name, slug, legacy subcategories, isActive, sortOrder.
  - `Subcategory`: name, slug, category ref, isActive, sortOrder, unique category+slug index.
  - `PushToken`: token, platform, userId, deviceId, valid/disabled, lastError/lastTriedAt, lastSeenAt/lastHeartbeatAt, lastLocation/accuracy/speed, interests, projectId; mehrere Aktivitaets-/Geo-Indizes.
  - `OfferVisibility`: deviceToken, offerId, status `seen/notified/dismissed/snoozed`, inside, enter/exit timestamps, notify/remind/suppress fields; Dedupe-Methoden.
  - `Tester`: key, name, email, validatedAt, acceptedAt, ndaVersion, gateModalMessage, status.
  - `ClientDiagLog`: device/platform/appVersion/buildNumber/event/level/data/receivedAt, TTL-Index.
  - `NotificationLog`: userId, offerId, date.
- bekannte funktionierende Features nach Codeanalyse:
  - Express API mit CORS-Whitelist plus ENV-Erweiterung.
  - MongoDB-Verbindung ueber `MONGO_URI`.
  - Offer CRUD, Provider CRUD, Kategorien/Subkategorien und Uploads.
  - Aktive/nearby/geofence-Angebotssuchen inklusive Geo-Fallbacks.
  - Heartbeat-basierter Server-Geofence-Check mit OfferVisibility-Dedupe und Expo Push.
  - PushToken-Registrierung und Service-State-Sync.
  - Offer-Poller-Job beim Start, abschaltbar ueber `OFFER_POLLER_ENABLED=0`.
  - APK Redirect auf `APK_TARGET_URL` oder Default-Spaces-URL.
- bekannte offene Punkte/Risiken:
  - `backend/.env.example` nutzt jetzt StepsMatch-Beispieldomains in `CORS_ORIGINS`; echte Runtime-ENV bleibt separat zu pruefen und darf keine Secrets in Git enthalten.
  - Kein automatischer Test-Runner im Backend-Package definiert.
  - E-Mail-Verifikation haengt von Resend-ENV ab; Laufzeit nicht geprueft.
  - Schema-Migration ist teilweise sichtbar: `categoryId`/`subcategoryId` plus Legacy-Felder existieren parallel.
  - Der Backend-Worktree hat nach Commit `5a7a7d0` keine offenen tracked Diffs mehr; Frontend/Mobile/Assets/Legal bleiben separat dirty.

## 6. Infrastruktur und Konfiguration

- Env-Variablen, nur Namen:
  - Backend: `PORT`, `NODE_ENV`, `MONGO_URI`, `MONGODB_URI`, `DATABASE_URL`, `JWT_SECRET`, `ALPHA_MASTER_LOGIN_ENABLED`, `ALPHA_MASTER_EMAIL`, `ALPHA_MASTER_PASSWORD`, `CORS_ORIGINS`, `APK_TARGET_URL`, `OFFER_POLLER_ENABLED`, `DEFAULT_OFFER_RADIUS_M`, `HB_MAX_CHECK_DISTANCE_M`, `GEOFENCE_RENOTIFY_COOLDOWN_MS`, `REENTRY_MIN_GAP_MS`, `SERVER_MIN_ACCURACY_BOOST_M`, `SERVER_TOLERANCE_M`, `EXIT_BUFFER_M`, `PUSH_MAX_DISTANCE_M`, `PUSH_LAST_LOCATION_MAX_AGE_MS`, `PUSH_ACCURACY_BUFFER_MAX`, `PUSH_ACCURACY_TOKEN_CAP`, `OFFER_NOTIFY_RESET_ON_UPDATE`, `FRESH_RETRY_DELAYS_MS`, `DEBUG_OFFER_POLLER`, `GEOPUSH_DEBUG`, `EXPO_ACCESS_TOKEN`, `EXPO_PROJECT_ID`, `EXPO_PROJECT`, `PROJECT_ID`, `PUSH_ENFORCE_PROJECT_SCOPE`, `PUSH_GRACE_MINUTES`, `EXPO_PUSH_CHANNEL_ID`, `PUSH_CHANNEL_ID`, `PUSH_CATEGORY_ID`, `PUSH_PRIORITY`, `PUSH_SOUND`, `CLIENT_DIAG_TTL_SECONDS`, `DIAG_READ_TOKEN`, `NOTIF_SNOOZE_MINUTES`, `NOTIF_MUTE_MINUTES`, `NOTIF_GO_MUTE_MINUTES`, `EMAIL_VERIFICATION_TTL_MINUTES`, `EMAIL_VERIFICATION_RESEND_COOLDOWN_SECONDS`, `EMAIL_VERIFICATION_DEBUG`, `RESEND_API_KEY`, `RESEND_FROM_EMAIL`, `CLOUDINARY_URL`, `CLOUDINARY_CLOUD_NAME`, `CLOUDINARY_API_KEY`, `CLOUDINARY_API_SECRET`, `CLOUDINARY_FOLDER`, `SEED_TAG`, `SEED_PROVIDERS`, `SEED_UNI_QUERY`, `SEED_PLACE`.
  - Frontend: `VITE_API_BASE_URL`, `VITE_API_FALLBACK_URL`, `VITE_GOOGLE_MAPS_API_KEY`, `VITE_GOOGLE_MAPS_MAP_ID`.
  - Mobile/Expo: `EXPO_PUBLIC_API_BASE_URL`, `EXPO_PUBLIC_GOOGLE_MAPS_ANDROID_KEY`, `EXPO_PUBLIC_GOOGLE_DIRECTIONS_KEY`, `EXPO_PROJECT_ID`, `EXPO_PROJECTID`, `EXPO_PROJECT`, `EXPO_PUSH_CHANNEL_ID`, `NODE_ENV`, `EXPO_OS`.
- Datenbank-/Index-Hinweise:
  - `Offer.location`, `Provider.location`, `PushToken.lastLocation` nutzen `2dsphere`.
  - `OfferVisibility` hat unique Index auf `deviceToken + offerId`.
  - `ClientDiagLog` nutzt TTL ueber `createdAt`.
  - `backend/scripts/create-indexes.js` erstellt mindestens Offer-Indizes fuer `location_2dsphere`, `subcategory_1`, `provider_1`.
- Push-/FCM-/Expo-Hinweise:
  - Expo Project ID in App-Config: `08559a29-b307-47e9-a130-d3b31f73b4ed`.
  - Channel IDs: `stepsmatch-bg-location-task`, `offers-v2`, `stepsmatch-nearby-attention-v1`, ausserdem `stepsmatch-default-v2`.
  - Task IDs: `stepsmatch-bg-location-task`, `stepsmatch-geofence-task`, `stepsmatch-heartbeat-fetch`.
  - Firebase/Google Services Dateien sind im Mobile/Android-Bereich vorhanden; Inhalte nicht in Markdown aufnehmen.
- Deployment-Hinweise:
  - Backend DO App Platform: Working Directory `backend`, Build `npm ci`, Run `npm start`, Health `/api/health`.
  - Frontend DO Static/App Platform: Working Directory `frontend`, Build `npm ci && npm run build`, Output `dist`; fuer React-Router-Direktaufrufe muss in der Static-Site-Konfiguration `Catchall`/`catchall_document` auf `index.html` gesetzt sein.
  - Frontend-API-Fallback zeigt aktuell auf `https://lobster-app-ie9a5.ondigitalocean.app/api` fuer bekannte Ultreia/DO Hosts.
  - APK Download via Backend `/apk` Redirect auf `APK_TARGET_URL`.
  - `.github/workflows/project-analysis.yml` ist manuell und veraendert externe Services nicht, nutzt aber GitHub Secret `CLONE_URL`.

## 7. Aktueller Funktionsstand

Nach Code- und Dateianalyse funktioniert wahrscheinlich:

- Backend startet mit MongoDB-URI, registriert API-Routen, bietet Healthchecks und startet optional den Offer-Poller.
- Anbieter koennen sich registrieren/anmelden, Provider und Angebote im Web verwalten und Bilder ueber Cloudinary hochladen, sofern ENV korrekt gesetzt ist.
- Web-Frontend zeigt Landing-/Info-/Legal-Seiten, Tester-Gate/NDA, Auth, Dashboard, Admin-Kategorien und Angebotskarte.
- Mobile-App kann User registrieren/anmelden, E-Mail-Verifikation abbilden, Interessen speichern, Standort-/Push-Permissions anfordern, Angebote laden, Details anzeigen und Navigation starten.
- Push-Kette ist breit implementiert: Token-Registrierung, Background Location, Heartbeat, Geofencing, OfferVisibility-Dedupe, serverseitige Expo Pushes, lokale Notifications und Diagnostics.
- Strong-Notification-Teststand 2026-06-23: Der Profil-Testbutton bleibt sichtbar, zusaetzlich existiert ein lokaler ADB-Deep-Link `stepsmatch://test-strong-notification`. Der Deep-Link triggert keine Remote-Pushes, kein Backend und keine DB-Mutation; Logcat zeigt `strongNearbyIntent buttonlessTrigger`, `permissionStatus=granted`, Scheduling auf `stepsmatch-nearby-attention-v1` und `scheduled ok`.
- Android-Device-Beleg 2026-06-23: Nach Release-Build/Install zeigt `dumpsys notification` fuer den Test `Notification(channel=stepsmatch-nearby-attention-v1)`, Titel `StepsMatch Naehe-Test`, Text `Diese Notification nutzt den starken Nearby-Channel.`, effektiven Channel mit Importance 5, Sound, Licht und Vibration `[0, 500, 180, 500, 180, 900]`. Vorher fiel auf, dass `channelId` fuer lokale Expo-Time-Interval-Notifications im Trigger stehen muss; nur Content/Data landete auf dem Expo-Fallback-Channel.
- Strong-Notification-Navigationsfix 2026-06-23: Der lokale Strong-Test hatte noch Offer-Notification-Signale (`offer-go-v2`/ungefilterte Response-Handler) und konnte nach Tap faelschlich wie eine echte Offer-Notification behandelt werden, bis hin zu `GET /offers/:id failed 500`. Fix: Strong-Test-Payloads sind jetzt `kind=strongNotificationTest`, `testOnly=true`, `noNavigation=true` und ohne Offer-Action-Kategorie; Notification-Response- und Initial-Notification-Handler ignorieren diese Testpayloads vor jeder Offer-Navigation. ADB-Retest bestaetigt Deep-Link-Scheduling, Android-Record auf `stepsmatch-nearby-attention-v1`, Tap-Log `testNotificationResponse ignored` und keinen `/offers/:id`-Request. Learning fuer spaetere Produkte getrennt halten: Test-/Diagnostics-Notifications duerfen nie wie echte Place-/Offer-Notifications behandelt werden; Response-Handler brauchen klare Typen wie test, critical_need, normal_need, discovery; Platzhalter wie `:id` duerfen nie in navigierbaren Payloads stehen.
- Angebotsmatching beruecksichtigt Ort, Zeitfenster, Datum/Wochentage, Radius und Interessen zumindest an mehreren Stellen server- und clientseitig.
- Release-Doku vom 2026-04-01 behauptet erfolgreiche Checks fuer Mobile-Lint, Frontend-Lint/Build, `node --check backend/server.js` und Prod-Smoke gegen `/api/health`, `/api/ping`, `/api/_readyz`, `/apk`; in dieser Bestandsaufnahme wurde das nicht erneut ausgefuehrt.

## 8. Bekannte Risiken / Unklarheiten

- Nicht getestet: keine Builds, Tests, API-Calls, DB-Verbindungen oder Deployments wurden in dieser Aufgabe ausgefuehrt.
- Dirty Working Tree: aktueller Stand ist nicht sauber committet. Die Analyse beschreibt den lokalen Arbeitsbaum, nicht nur `HEAD`.
- Viele untracked temporäre Artefakte koennen den Projektzustand vernebeln und sollten gesichtet/ignoriert/entfernt werden.
- Projekt-/Branding-Divergenz: StepsMatch.com als Projektname, aber Code, Domains, Texte und Deploy-Doku enthalten viel `Ultreia.app`/Camino.
- Domains/CORS/Fallbacks nennen `ultreia.app` und DigitalOcean Default-Hosts; Ziel-Domain StepsMatch.com muss konzeptionell abgeglichen werden.
- Android Package-Konflikt zwischen alter Doku und aktueller App-Konfiguration.
- Potenziell sensible Konfigurationsartefakte im Repo: Google/Firebase-Dateien und ein AndroidManifest mit Google Maps Key-Metadaten. Keine Werte dokumentieren; Key-Scope/Restriktionen pruefen.
- Mobile Background-Services koennen auf Android durch OEM-Akku-Management, Doze, fehlende "Immer erlauben"-Location oder deaktivierte Notification Channels ausfallen.
- Strong-Notification-Wahrnehmung bleibt manuell zu pruefen: naechster Schritt ist APK/Device-Test mit Screen-off/Hosentasche; automatisiert bestaetigt ist der native Channel/Record, nicht die physische Lautstaerke/Vibration in Alltagssituation.
- Native Manifest enthaelt Berechtigungen, deren Bedarf nicht aus der kurzen Analyse belegt ist (`RECORD_AUDIO`, `SYSTEM_ALERT_WINDOW`, Storage).
- `mobile/README.md` ist generisch und nicht projektspezifisch.
- `.gitignore` ignoriert lokale Logs, Screenshots, UI-Dumps, Temp-Skripte, APK/AAB-Artefakte und Credential-Dateitypen; `.env.example` bleibt ausdruecklich versionierbar.
- Git-Cleanup-Check vom 2026-06-17: lokale `.env*` und Release-Keystore-Dateien sind durch `.gitignore` abgedeckt; tracked bleiben jedoch Google/Firebase-Konfigurationsdateien und `mobile/android/app/debug.keystore`, die vor einem Commit/Release bewusst geprueft werden muessen.
- Backend-Resttreffer-Check vom 2026-06-19: `backend/server.js` enthaelt keine Ultreia-/Camino-Treffer mehr; das zuvor kritische Config-/URI-Risiko in `backend/scripts/ensure-indexes.mjs` wurde entfernt.
- Zeichencodierung in einigen Markdown-/JS-Ausgaben wirkt teilweise mojibake-betroffen; echte UX-Texte in App/Web muessten separat visuell geprueft werden.

## 9. Empfohlene nächste Schritte

1. Git-Arbeitsbaum aufraeumen: untracked Screenshots, UI-Dumps, Logs und Temp-Skripte klassifizieren; benoetigte Artefakte nach `docs/` oder Testordner verschieben, Rest ignorieren/entfernen.
2. Branding-/Domain-Entscheidung festlegen: StepsMatch.com vs. Ultreia.app/Camino. Danach Code, CORS, Fallbacks, Legal-Texte und App-Strings konsistent machen.
3. Package-ID final klaeren: `com.ecily.stepsmatch` als aktuelle App-ID bestaetigen oder bewusst migrieren; alte Doku/ADB-Befehle aktualisieren.
4. Secret-/Key-Audit durchfuehren: Google/Firebase/Cloudinary/Resend/Expo Keys auf Repo-Leaks pruefen, Werte rotieren falls noetig, API-Key-Restriktionen setzen.
5. Lokale risikoarme Checks ausfuehren, sobald Arbeitsbaum verstanden ist: `npm run lint` in `frontend` und `mobile`, `npm run build` in `frontend`, `node --check backend/server.js`.
6. Backend-Health und API-Smoke lokal/gegen Staging nur mit sicheren Read-only Calls pruefen.
7. Datenmodell-Migration Category/Subcategory dokumentieren und mit Backfill-/Rollback-Plan versehen.
8. Mobile Background/Push E2E-Testplan aus `docs/TESTLAUF_E2E_WEB_TO_APP.md` aktualisieren und auf aktuelle Package-ID/Domain anpassen.
9. `README.md`, `mobile/README.md` und `DEPLOY_DO_CHECKLIST.md` an den aktuellen Projektstand angleichen.

## 10. Arbeitsregeln für Codex

- Vor jeder weiteren Arbeit zuerst `docs/STEPSMATCH_CONTEXT.md` lesen.
- Nach relevanten Aenderungen diese Datei schlank aktualisieren.
- Keine Arbeiten ausserhalb von `C:\coding\stepsmatch`.
- Keine Vermischung mit Kaufklug oder anderen Projekten.
- Keine Secrets in Logs, Commits oder Markdown schreiben.
- Vor Codeaenderungen immer zuerst den aktuellen Git-Status pruefen.
- Build/Deploy/ADB/DB-Mutationen nur nach expliziter Freigabe oder wenn eindeutig lokal und risikoarm.
- Wegen Live-Betrieb auf DigitalOcean und live angebundener MongoDB: kein Push, kein Deploy und keine DB-Mutation ohne explizite Freigabe.
- Bestehende uncommitted Aenderungen als User-Arbeitsstand behandeln und nicht revertieren.
- Am Ende jeder Aufgabe berichten:
  - Ampel: Gruen/Gelb/Rot
  - Kurzfazit
  - geaenderte Dateien
  - Tests/Checks
  - Git-Status
  - offene Punkte
